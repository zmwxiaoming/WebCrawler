拉钩某个分类 第一页 第二页 。。。。总页数 参数分析

页数在页面中显示为：class="Pagination"，在源代码中在方法$('.Pagination').pager({})，此方法中各字段含义分别为
currPage: 当前所在页面,
pageNOName: 对应URL地址pn字段名，默认值"pn",
form: 默认为"searchForm",
pageCount: 总页数,
pageSize:  上一页与下一页中间显示按钮数量，默认为5

URL字段解析：

  字段连接规则：
    list_字段完成后使用'?'连接，其余各个字段之间使用'&'连接，除list_字段外，其他字段与参数之间的连接需要'='连接如：pn=10
  当某字段不存在参数的时候，仍需'='如：假设pl字段没有参数，后一个字段为gj则URL应该为:pl=gj=...

  字段对应含义解析：  
URL字段前缀
	后接参数说明
list_
	搜索栏输入内容，原封不动
kd
	根据搜索栏内容适当转换字符串，如：空格会被转换为加号，一个空格转换一个，可以出现连续多个加号
spc
	搜索栏分类，1代表职位，4代表公司
pl
	源代码mb10类中title字段包含的值，只匹配一个字符串，就是具体工作左半部中城市左边的标题部分，如JAVA[北京]中的JAVA就是title字段的内容，这个字段会和这里的内容匹配
gj
	工作经验
xl
	最低学历
yx
	月薪
gx
	工作性质
st
	发布时间
labelWords
	当搜索栏中输入的字符串为系统提示的字符串时，该字段值为sug，当用户点击热门搜索中的字符串时，该字段会为Hot
lc
	暂无无结果。。。
workAddress
	暂无无结果。。。
city
	工作城市，参数为中文
